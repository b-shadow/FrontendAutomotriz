import { createContext } from 'react'

/**
 * ThemeContext Estados:
 * - 'light': Modo claro (blanco + morado)
 * - 'dark': Modo oscuro (fondo oscuro + morado)
 */
export const ThemeContext = createContext()
